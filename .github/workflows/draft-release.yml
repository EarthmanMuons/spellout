# This file is generated by .github/cue/ci_tool.cue; DO NOT EDIT!

name: draft-release
"on":
  push:
    tags:
      - v[0-9]+.[0-9]+.[0-9]+
permissions:
  contents: write
jobs:
  draft_release:
    name: create draft release
    runs-on: ubuntu-latest
    outputs:
      upload_url: ${{ steps.gh_release.outputs.upload_url }}
      url: ${{ steps.gh_release.outputs.url }}
    steps:
      - name: Checkout source code
        uses: actions/checkout@8e5e7e5ab8b370d6c329ec480221332ada57f0ab
      - id: gh_release
        name: Create a new GitHub draft release
        uses: softprops/action-gh-release@de2c0eb89ae2a093876385947365aca7b0e5f844
        with:
          draft: true
          generate_release_notes: true
          discussion_category_name: announcements
      - name: Annotate workflow run with draft release URL
        run: 'echo "#### :shipit: Opened draft release for v${{ github.ref_name }}: ${{ steps.gh_release.outputs.url }}" >> "$GITHUB_STEP_SUMMARY"'
  upload_artifacts:
    name: upload release artifacts
    needs:
      - draft_release
    runs-on: ubuntu-latest
    steps:
      - name: Building release artifacts
        run: echo "Uploading to ${{ needs.draft_release.outputs.upload_url }}"
